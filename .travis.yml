language: c
compiler:
  - clang
  - gcc
before_install:
  - pip install --user cpp-coveralls
script:
  - make && make tests && env LD_LIBRARY_PATH=./ ./test
after_success:
  - coveralls --gcov-options '\-lp'